import React from 'react';
import Button from '@mui/material/Button';

const GalleryButton = () => {
    return ( 
    <Button variant='text' className='button'>Gallery</Button>
    );
}
 
export default GalleryButton;